# Theory

The theoretical write-up is now provided as LaTeX in [theory.tex](/home/samuelemara/Joint3DGS/theory.tex).

You can compile it with a standard LaTeX toolchain, for example:

```bash
pdflatex /home/samuelemara/Joint3DGS/theory.tex
```
